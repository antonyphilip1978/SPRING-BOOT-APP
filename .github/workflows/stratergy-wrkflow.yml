name: stratergy work flow

on:
  workflow_dispatch:
      inputs:
        opened:
          description: hello
          required: true
jobs:
    self-approval-check:    
      runs-on: ubuntu-latest
      strategy:
        matrix:
          service_account: [ifd, ift, dbc]
          include:
            - service_account: 'ifd'
              project: asdf 
            - service_account: 'ift'
              project: asdf 
            - service_account: 'dbc'
              project: asdf 
      steps:
        - id: copy
          name: hello
          run: |-
            echo ${{ matrix.service_account }}

            